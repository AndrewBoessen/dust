defmodule Dust.Mesh do
  @moduledoc """
  Top-level module for the Dust mesh network layer.

  Submodules:

    * `Dust.Mesh.KeyExchange`  — master-key distribution across peers
    * `Dust.Mesh.NodeRegistry` — tracks the status of all nodes in the mesh
  """
end

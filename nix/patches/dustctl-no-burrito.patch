--- a/apps/dust_cli/mix.exs
+++ b/apps/dust_cli/mix.exs
@@ -26,7 +26,6 @@
   defp deps do
     [
       {:jason, "~> 1.4"},
-      {:burrito, "~> 1.0"},
       {:owl, "~> 0.12"},
       {:websockex, "~> 0.4.3"}
     ]
@@ -35,16 +34,8 @@
   defp releases do
     [
       dustctl: [
-        steps: [:assemble, &Burrito.wrap/1],
-        burrito: [
-          targets: [
-            linux_x86_64:   [os: :linux,   cpu: :x86_64],
-            linux_aarch64:  [os: :linux,   cpu: :aarch64],
-            macos_x86_64:   [os: :darwin,  cpu: :x86_64],
-            macos_aarch64:  [os: :darwin,  cpu: :aarch64],
-            windows_x86_64: [os: :windows, cpu: :x86_64]
-          ]
-        ]
+        steps: [:assemble],
+        applications: [dust_cli: :permanent]
       ]
     ]
   end
--- a/apps/dust_cli/lib/dust_cli/application.ex
+++ b/apps/dust_cli/lib/dust_cli/application.ex
@@ -3,13 +3,12 @@
   use Application

   def start(_, _) do
-    case Burrito.Util.Args.get_bin_path() do
-      :not_in_burrito ->
-        # Running under mix test or mix run — do not execute the CLI
+    case System.get_env("DUSTCTL_ARGV") do
+      nil ->
         Supervisor.start_link([], strategy: :one_for_one)

-      _bin_path ->
-        args = Burrito.Util.Args.argv()
+      raw ->
+        args = String.split(raw, "\n", trim: true)
         exit_code = Dust.CLI.run(args)
         System.halt(exit_code)
     end
